{
  "name": "safelight-spektrafilm-support",
  "version": "2.1.0",
  "description": "Spektrafilm Support extension for SafeLight — real-time, per-pixel spectral film simulation (negative → enlarger → print → scan) run live on the GPU.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "keywords": ["safelight-extension"],
  "license": "GPL-3.0-or-later"
}
